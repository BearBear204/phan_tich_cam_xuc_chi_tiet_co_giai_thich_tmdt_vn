Main source tree only.

Included here:
- `crawl/`: main Shopee crawler
- `preprocess/`: main cleaning pipeline
- `processing/`: main dataset builders for document-level and ABSA
- `training/`: main training scripts for the three paper models

Older experiments, alternate branches, evaluation helpers, and benchmark tooling were moved outside `datn/`.
